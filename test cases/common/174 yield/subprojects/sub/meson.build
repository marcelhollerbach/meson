project('subbie', 'c')

assert(get_option('super_alone') == 'three', 'Unshared option has wrong value in subproject.')
assert(get_option('super_shared') == 'two', 'Shared option has wrong value in subproject.')
